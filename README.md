# domless
set yourself free

## Setup

### Install dependencies:
`$ npm install`

### Run local server (port 8080):

`$ npm run serve`

### Run active dev rebuild:

`$ npm run watch`

### Run tests:

`$ npm run test`

That's it!
